#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_nopt@1.0.10@nopt/bin/nopt.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_nopt@1.0.10@nopt/bin/nopt.js" "$@"
  ret=$?
fi
exit $ret
