#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_nodemon@1.11.0@nodemon/bin/nodemon.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_nodemon@1.11.0@nodemon/bin/nodemon.js" "$@"
  ret=$?
fi
exit $ret
