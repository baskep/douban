#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_touch@1.0.0@touch/bin/touch.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_touch@1.0.0@touch/bin/touch.js" "$@"
  ret=$?
fi
exit $ret
