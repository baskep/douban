#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_grunt-cli@1.2.0@grunt-cli/bin/grunt" "$@"
  ret=$?
else 
  node  "$basedir/../../../_grunt-cli@1.2.0@grunt-cli/bin/grunt" "$@"
  ret=$?
fi
exit $ret
